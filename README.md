# UC‑9: Group Bogies by Type

## Overview
Groups bogies by their type (name) using Stream `groupingBy`.

## What It Does
- Creates bogies list with repeated types
- Groups by name into a Map<String, List<Bogie>>
- Prints groups and capacities inside each group

## Purpose
To demonstrate classification/grouping using Java Streams.

