# UC‑10: Count Total Seats in Train

## Overview
Uses Stream `reduce()` to total the seating capacity of all bogies.

## What It Does
- Creates bogie list
- Extracts capacity using map()
- Adds capacity using reduce()
- Prints total seats

## Purpose
To demonstrate aggregation using the Stream reduce() method.
